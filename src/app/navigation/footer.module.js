(function () {
    'use strict';

    angular
        .module('app.footer', [])
        .config(config);

    /** @ngInject */
    function config() {
        // $translatePartialLoaderProvider.addPart('app/toolbar');
    }
})();
