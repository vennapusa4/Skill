(function () {
    'use strict';

    angular
        .module('app.header', [])
        .config(config);

    /** @ngInject */
    function config() {
        // $translatePartialLoaderProvider.addPart('app/toolbar');
    }
})();
